{
  "name": "create-dev-expo",
  "version": "1.0.1",
  "type": "module",
  "bin": {
    "create-dev-expo": "bin/index.js"
  },
  "scripts": {
    "build": "bun build ./index.ts --target=node --outfile ./bin/index.js",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
